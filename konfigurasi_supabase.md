
-- ... (bagian awal skrip sama)

-- 3. Membuat tabel 'feature_flags' jika belum ada.
CREATE TABLE IF NOT EXISTS public.feature_flags (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  feature_name TEXT NOT NULL UNIQUE,
  is_enabled BOOLEAN DEFAULT TRUE NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW() NOT NULL
);

-- Mengisi data awal untuk fitur-fitur yang ada.
INSERT INTO public.feature_flags (feature_name, is_enabled) VALUES
  ('rpm_generator', TRUE),
  ('question_card_generator', TRUE),
  ('rps_generator', TRUE),
  ('rpd_generator', TRUE),
  ('atp_generator', TRUE),
  ('journal_generator', TRUE),
  ('ck_generator', TRUE), -- BARU: Memastikan fitur CK terdaftar di database
  ('client_api_key_input', TRUE),
  ('payment_system', TRUE),
  ('donation_card', TRUE)
ON CONFLICT (feature_name) DO NOTHING;

-- ... (lanjutkan sisa skrip)
